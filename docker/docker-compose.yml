services:
    server:
        build:
            context: ../
            dockerfile: docker/Dockerfile
        container_name: statsfm-server
        restart: unless-stopped
        environment:
            LISTEN_ADDR: ":8080"
        ports:
            - "2812:8080"
